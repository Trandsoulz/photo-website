/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["api.unsplash.com", "images.unsplash.com", "source.unsplash.com", "images.pexels.com", ],
  },
};

module.exports = nextConfig;
